---
docker-build:
  stage: build
  image: docker
  services:
  - docker:dind
  script:
  - docker build -t my-image .

docker-publish:
  stage: publish
  image: docker
  services:
  - docker:dind
  script:
  - docker push my-image
