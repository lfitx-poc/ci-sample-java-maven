---
docker-build:
  stage: build
  image: docker
  services:
  - docker:dind
  script:
  - docker build -t ${CI_PROJECT_NAME} .

docker-publish:
  stage: publish
  image: docker
  services:
  - docker:dind
  script:
    - docker push ${CI_PROJECT_NAME}:latest
